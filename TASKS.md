# TASKS — Life RPG

Актуальный план технической очистки после ревью кода. Продуктовые задачи ведутся в `BACKLOG.md`.

## P0 — корректность и изоляция данных

- [x] Проверять новый `skillId` при редактировании повторения: навык должен принадлежать текущему пользователю и быть активным.
  - [x] Добавить проверку в application-слой.
  - [x] Покрыть смену на чужой и архивный навык интеграционными тестами.
  - [x] Закрепить принадлежность навыка пользователю составным внешним ключом на уровне БД.
- [x] Сделать создание повторения и первой задачи атомарным: выполнять обе операции в одной транзакции.
- [x] Сделать архивацию навыка и связанных повторений атомарной: выполнять обе операции в одной транзакции.
- [x] Усилить проверку ссылочной целостности при импорте резервной копии.
  - [x] Проверять ссылки `userSkills → skills`.
  - [x] Проверять ссылки `streaks → taskTemplates`.
  - [x] Проверять `xpTransactions.sourceId` для завершений задач, завершений квестов и отмен.
  - [x] Добавить тесты импорта повреждённых и неполных резервных копий.

## P1 — логика и производительность

- [x] Добавить пакетную материализацию повторяющихся задач для диапазона дат.
  - [x] Загружать активные шаблоны один раз.
  - [x] Создавать недостающие задачи одной пакетной операцией.
  - [x] Перевести экран «Сегодня» на новый сценарий вместо семи отдельных вызовов.
- [x] Оптимизировать недельный прогресс: загружать XP за общий диапазон один раз и вычислять текущий, предыдущий и выбранный периоды в памяти.
- [x] Убрать неиспользуемый запрос просроченных задач из `weeklyQuestSummary`.
- [x] Исправить перенос сфокусированной задачи на другую дату.
  - [x] При переносе назначать свободную позицию или сбрасывать фокус, если целевой день заполнен.
  - [x] Блокировать и проверять позиции фокуса для целевого дня.
- [ ] После появления полноценного онбординга убрать runtime-вызовы `ensureWorkspace` и автоматическое создание `STARTER_SKILLS` — зависит от незавершённого раздела «Первый запуск и библиотека пакетов» в `BACKLOG.md`.

## P2 — удаление неактуального кода

- [x] Удалить неиспользуемый `src/components/ui/card.tsx`.
- [x] Удалить неиспользуемые функции `deleteTask`, `findUserById` и `maxCurrentStreak`.
- [x] Сделать `listRecentTransactions` внутренней функцией модуля.
- [x] Оставить документированный read API и закрепить его контрактным E2E-тестом.
- [ ] Добавить в CI проверку неиспользуемых файлов и экспортов через `knip`.
  - Требуется ручная установка зависимости: `npm install -D knip@6.23.0`.

## P2 — декомпозиция крупных модулей

- [x] Разделить `src/application/profile/import-data.ts` на проверку/импорт резервной копии и планирование/импорт контент-пака; общие контракты вынести в client-safe модуль.
- [x] Разделить `profile-settings.tsx` на настройки интерфейса и часового пояса, импорт/экспорт данных и управление сессией.
- [x] Вынести общие поля задачи/повторения и UI настройки расписания из drawer-компонентов.
- [x] Вынести редактор и список шагов из крупных компонентов квестов.

## Порядок выполнения

1. P0: целостность данных и транзакции.
2. Пакетная материализация задач и корректный перенос фокуса.
3. Оптимизация недельного прогресса.
4. Удаление мёртвого кода и решение по read API.
5. Декомпозиция крупных модулей.

## Идеи

Новые продуктовые идеи фиксировать в `BACKLOG.md` и не реализовывать без отдельного запроса.
