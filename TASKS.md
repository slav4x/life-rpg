# TASKS — Life RPG

Прогресс по роадмапу из `SPEC.md` §20. Работаем строго по этапам: следующий
игровой слой не начинаем, пока текущий не проверен.

Легенда: `- [x]` готово · `- [ ]` не начато · 🚧 в работе.

---

## Этап 0 — фундамент проекта

- [x] Инициализировать Next.js с TypeScript strict.
- [x] Подключить Tailwind CSS и shadcn/ui.
- [x] Настроить lint, typecheck, Vitest и Playwright.
- [x] Добавить Dockerfile, Docker Compose и Caddyfile.
- [x] Настроить `.env.example` и типизированную проверку окружения.
- [x] Создать `README.md`, `CHANGELOG.md` и `TASKS.md`.

**Результат:** приложение собирается локально и в Docker, healthcheck отвечает успешно.

### Проверено

- `npm run lint` — без ошибок.
- `npm run typecheck` — без ошибок.
- `npm run build` — успешная production-сборка (standalone).
- `npm run test` — unit-тесты проходят (env-валидация).

### Требует ручной проверки

- `docker compose build` / `docker build` и ответ `/api/health` в контейнере.
- `npm run test:e2e` — предварительно `npx playwright install`.
- Локальный Node 23 вне поддерживаемого диапазона Next 16 (нужен `>=20.19`/`22.13+`/`24`).
  Dev/сборка работают, но рекомендуется LTS; Docker уже использует `node:24-alpine`.

---

## Этап 1 — база и Telegram auth

- [x] Подключить PostgreSQL и Drizzle.
- [x] Создать таблицы пользователей и сессий.
- [x] Реализовать проверку Telegram `initData`.
- [x] Добавить allowlist Telegram ID.
- [x] Реализовать HTTP-only сессию и logout.
- [x] Добавить auth fixture для тестов.

**Результат:** владелец входит из Telegram, посторонний пользователь получает `403`.

### Проверено

- `npm run typecheck`, `npm run lint`, `npm run build` — без ошибок.
- `npm run test` — 18 тестов (unit: HMAC-проверка `initData`, серии/токены; integration
  против реального PostgreSQL 17: создание user+session, отсутствие дублей при
  повторном входе, `403` для чужого ID, ревокация сессии при logout).
- HTTP-смоук на запущенном приложении: allowlisted → `200` + HttpOnly-cookie
  `life_rpg_session`; чужой → `403`; поддельный `initData` → `401`; невалидное тело → `400`;
  logout → `200`.

### Заметки / trade-offs

- `DATABASE_URL` и `TELEGRAM_BOT_TOKEN` теперь нужны в рантайме; в env-схеме оставлены
  optional, чтобы `next build` не требовал секретов (в Docker — `SKIP_ENV_VALIDATION=1`).
- Cookie `SameSite=Lax` по SPEC. В Telegram Web (desktop, cross-site iframe) Lax-cookie не
  отправляется; на мобильных клиентах (основная цель) работает. Пересмотреть при
  необходимости десктоп-поддержки.
- Интеграционные тесты запускаются только при заданном `TEST_DATABASE_URL`
  (иначе `describe.skipIf` их пропускает), поэтому `npm run test` зелёный без БД.
- Применение миграций в проде (`drizzle-kit migrate` требует dev-зависимостей и исходников)
  дорабатывается на Этапе 6; для dev/CI есть `npm run db:migrate`.
- Для локальной разработки без Telegram есть `DEV_AUTH_BYPASS=1` (только `npm run dev`,
  игнорируется в production) — авто-вход мок-пользователем через `/api/auth/dev-login`.

## Этап 2 — вертикальный игровой цикл

- [x] Добавить справочник характеристик.
- [x] Реализовать навыки.
- [x] Реализовать разовые задачи.
- [x] Реализовать завершение в транзакции.
- [x] Добавить XP-журнал и формулу уровней.
- [x] Собрать экран «Сегодня».
- [x] Показать результат начисления XP.

**Результат:** пользователь создаёт действие, завершает его и видит изменение общего уровня, навыка и характеристики.

### Проверено

- `npm run typecheck`, `npm run lint`, `npm run build` — без ошибок.
- `npm run test` — 39 тестов. Unit: формулы уровня (таблица §5.1) и XP (множители/округление,
  25% характеристике), локальная дата по таймзоне. Integration против PostgreSQL:
  начисление в global/skill/attribute одной транзакцией + обновление кэшей, идемпотентный
  повтор без двойного начисления, level-up на пороге, множитель сложности.
- Живой цикл через HTTP (dev-сервер): dev-login → сид 8 стартовых навыков → создание
  действия (`hard`, base 50) → завершение → `+65 XP` (global/skill), `+16 XP` характеристике,
  повтор `alreadyCompleted`; журнал `xp_transactions` (3 строки ×1.30) и кэши совпадают.

### Заметки / trade-offs

- XP-журнал — источник истины; общий XP считается суммой global-транзакций (без кэша на `users`).
- 6 характеристик засеяны миграцией `0002_seed_attributes.sql`; стартовые навыки —
  лениво при первом входе (`ensureWorkspace`).
- В scope Этапа 2 только разовые задачи. Серии, шаблоны, отмена выполнения — Этап 3;
  квесты и достижения (`levelUp`/поля результата зарезервированы) — Этап 4.
- Нижняя навигация и остальные экраны появятся вместе со своими этапами.

## Этап 3 — повторения и серии

- [x] Добавить шаблоны задач.
- [x] Реализовать ежедневное и недельное повторение.
- [x] Реализовать ленивое создание задач дня.
- [x] Реализовать серии.
- [x] Реализовать отмену выполнения.
- [x] Проверить конкурентные запросы и идемпотентность.

**Результат:** ежедневный список создаётся автоматически при открытии без cron и дублей.

### Проверено

- `npm run typecheck`, `npm run lint`, `npm run build` — без ошибок.
- `npm run test` — 58 тестов. Unit: серии (границы дат, пропуски, best), применимость
  recurrence по дням недели, ISO-день недели и арифметика дат. Integration против PostgreSQL:
  ленивое создание задач из шаблона без дублей при **конкурентных** вызовах, фильтр по дням
  недели, игнор архивных шаблонов; revert с откатом XP (компенсирующие транзакции) и
  пересчётом серии с сохранением best; `nothing_to_revert`; идемпотентность при
  конкурентном завершении (row-lock).
- Живой flow (dev-сервер + psql): daily-шаблон → двойная загрузка «Сегодня» даёт **1** задачу
  → завершение `+16 XP`, серия `1/1` → revert: задача `pending`, серия `0/1` (best сохранён),
  `reverted_at` выставлен. UI: recurrence-селектор с переключателями дней недели, кнопки
  «Отменить» и индикатор серии 🔥.

### Заметки / trade-offs

- Серия считается пересчётом по фактическим (не-reverted) выполнениям шаблона — консистентно
  для complete и revert, устойчиво к выполнению не по порядку. `best_count` не уменьшается.
- Ленивое создание идемпотентно и конкурентно-безопасно через партиальный уникальный индекс
  `(user_id, local_date, template_id)` + `insert … on conflict do nothing`.
- Дни недели: ISO 1=Пн … 7=Вс. Просроченные задачи не переносятся (остаются на своей дате).
- Управление шаблонами (список/архив) есть в API; экран управления — Этап 5 (Профиль).
- Редактирование задачи из шаблона («только эта / будущие») не реализовано — по SPEC §6.3
  это UX-уточнение; отложено.

## Этап 4 — квесты и достижения

- [x] Реализовать CRUD квестов.
- [x] Добавить шаги и прогресс.
- [x] Реализовать завершение и XP-награду.
- [x] Добавить стартовый набор достижений.
- [x] Показывать новые достижения после действия.

**Результат:** долгосрочная цель проходит путь от создания до завершения и награды.

### Проверено

- `npm run typecheck`, `npm run lint`, `npm run build` — без ошибок.
- `npm run test` — 69 тестов. Unit: правила достижений (все типы), прогресс квеста и
  applyRequiredDone. Integration против PostgreSQL: завершение квеста с наградой и
  разблокировкой `first_quest`, идемпотентность (без двойной награды), авто-завершение при
  `manual_completion=false`, разблокировка `first_action` только один раз.
- Живой flow (dev + psql): создание квеста с шагами → toggle → завершение `+300 XP`,
  `levelUp 1→2`, достижения `first_action`+`first_quest`, идемпотентный повтор; журнал —
  одна `quest_completion` транзакция.
- Визуально: экран «Квесты» (табы Активные/Завершённые/Архив, карточки с прогрессом),
  экран квеста (шаги-чекбоксы, прогресс, подтверждение завершения), нижняя навигация,
  toast достижений.

### Заметки / trade-offs

- Награда квеста начисляется в общий XP (global). `attribute_id` квеста — категоризация.
- 8 достижений засеяны миграцией `0006_seed_achievements.sql`; проверка синхронная после
  завершения задачи/квеста, разблокировка идемпотентна (PK `user+achievement`).
- Достижения проверяются ретроспективно: например, `first_action` может открыться при
  первом завершении квеста, если задачи уже выполнялись до появления достижений.
- Нижняя навигация: Сегодня и Квесты активны; Навыки/Прогресс/Профиль — на Этапе 5.
- Экран списка достижений — в Профиле (Этап 5); сейчас показываются в toast + `GET /api/achievements`.
- `tasks.quest_step_id` добавлен в схему (SPEC §10); создание задачи из шага квеста — позже.

## Этап 5 — статистика и профиль

- [x] Собрать экран навыков.
- [x] Добавить историю XP.
- [x] Реализовать статистику за 7/30 дней и всё время.
- [x] Добавить экран профиля и характеристик.
- [x] Добавить настройки темы и часового пояса.
- [x] Реализовать экспорт JSON.

**Результат:** пользователь видит накопительный прогресс и может выгрузить свои данные.

### Проверено

- `npm run typecheck`, `npm run lint`, `npm run build` — без ошибок.
- `npm run test` — 73 теста. Integration против PostgreSQL: агрегация прогресса за 7 дней
  (totalXp/completedTasks/daily/attributes), профиль (6 характеристик + 8 достижений),
  экспорт (сериализуемый JSON, `telegramId` строкой, без bigint), отказ на невалидной таймзоне.
- Server-side (node fetch на dev): `/skills`, `/progress`, `/profile` — 200 со всеми блоками;
  `/api/export` — 200, `attachment`, валидный JSON (4 задачи, 67 XP-транзакций).

### Заметки / trade-offs

- Нижняя навигация полностью включена (5 вкладок).
- Прогресс: дневной график и «XP за период» строятся из выполнений задач по локальной дате;
  награды квестов входят в общий XP, но не в дневную разбивку. Таблица XP-транзакций — источник истины.
- Тема через `next-themes` (класс `.dark`, localStorage) + запись выбора в `users.theme`
  через `PATCH /api/profile`; смена таймзоны пересчитывает локальную дату.
- Экспорт исключает `sessions` (хранят hash токенов). Графики — `recharts` (client island).
- Редактирование навыка меняет название/описание; смена характеристики — только при создании.

## Этап 6 — production readiness

- [x] Завершить unit, integration и E2E тесты.
- [x] Проверить mobile viewport и Telegram safe areas.
- [x] Добавить security headers.
- [x] Настроить production deploy.
- [x] Настроить внешний backup PostgreSQL (`scripts/backup.sh`, `scripts/restore.sh`).
- [x] Выполнить тестовое восстановление.
- [x] Проверить реальный запуск через BotFather.

**Результат:** стабильный персональный MVP работает на собственном сервере.

### Проверено

- `npm run typecheck`, `npm run lint`, `npm run build` — без ошибок.
- `npm run test` — 104 теста (unit + integration против PostgreSQL), включая
  DB-level ограничения и политику дублей. Playwright: 9 E2E-сценариев через dev-bypass.
- Security headers на живом сервере: `X-Content-Type-Options`, `Referrer-Policy`,
  `X-DNS-Prefetch-Control`, `Permissions-Policy` (без `X-Frame-Options` — для Telegram);
  HSTS добавляется в production.
- Production-миграции: собран лёгкий `migrator`-стейдж, `docker compose config` валиден,
  образ применяет миграции к БД (`migrate` — one-off, без гонок при старте app).
- **Тестовое восстановление**: `pg_dump -Fc` → restore в отдельную БД → счётчики совпали
  (users/tasks/xp_transactions идентичны источнику).

### Заметки / trade-offs

- Framing не ограничивается (нет `X-Frame-Options`/строгого `frame-ancestors`) — иначе
  ломается встраивание Mini App в Telegram.
- `migrate`-сервис использует стадию `builder`-lite (`target: migrator`) с dev-зависимостями;
  app стартует только после `service_completed_successfully` миграций.
- Backup рассчитан на запуск через systemd timer/cron на хосте; ротация 7/4/6, минимум одна
  копия — off-site (по SPEC §17). Скрипты используют `docker compose exec`.
- Реальный запуск через BotFather и завершение этапа 6 подтверждены владельцем проекта.
- Post-MVP несостыковки и будущие доработки вынесены в [`BACKLOG.md`](./BACKLOG.md).
- Статусы, типы, сложность и XP защищены `CHECK`; активные навыки и неархивные
  шаблоны уникальны по нормализованному названию в пределах пользователя.

---

## Идеи / Future

Не реализуем без явного запроса (SPEC §5.9):

- Подробный список post-MVP доработок и идей контента: [`BACKLOG.md`](./BACKLOG.md).

- Главы жизни и боссы, титулы по направлениям, перки за достижения.
- Временные эффекты, синергии полезных действий.
- Еженедельные обзоры, AI-предложения квестов.
- Импорт данных из Health, календарей и финансовых сервисов.
