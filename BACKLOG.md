# Post-MVP backlog — Life RPG

Список несостыковок и ограничений, найденных после завершения этапа 6.

**Статус:** P0 и планирование/повторения выполнены. Следующий этап — P1 по квестам,
архивам и контент-паку v2.

Приоритеты:

- **P0** — влияет на корректность данных, прогресса или production-эксплуатацию;
- **P1** — заметный функциональный пробел или расхождение со `SPEC.md`;
- **P2** — UX, документация и технический долг.

## P0 — следующий проход корректности — ✅ выполнено

- [x] Определить и защитить правила выполнения задач по датам. Сейчас будущую задачу можно
      завершить заранее: XP записывается на будущую локальную дату, попадает в итог периода,
      но не отображается в графике до наступления даты. Минимум — запретить это на сервере и
      ограничить статистические запросы текущей датой.
- [x] Определить политику выполнения задним числом: разрешённый период, влияние на серию и
      отображение в истории. Нельзя оставлять неявную возможность ретроспективно менять серии
      и дневной прогресс без понятного правила.
- [x] Добавить отмену завершения квеста по модели отмены задачи: подтверждение, возврат квеста
      в активные, компенсирующая XP-транзакция и идемпотентность. Уже открытые достижения по
      текущей политике остаются полученными.
- [x] Перестать терять длительность повторяющегося действия. Поле `estimatedMinutes` есть в
      общей форме, но при выборе повторения не сохраняется в шаблон и будущие задачи. Добавить
      поле в схему шаблона, API, редактирование, материализацию, экспорт и контент-пак.

## P1 — следующий продуктовый этап

### Планирование и повторения — ✅ выполнено

- [x] Добавить обзор задач вместо навигации только стрелками по одному дню: просроченные,
      сегодня, ближайшие 7 дней и быстрый переход по календарю. Будущие задачи сейчас трудно
      найти, если пользователь не помнит назначенную дату.
- [x] Добавить границы действия шаблона: `startsOn` и необязательный `endsOn`. Создание
      повторения на будущую дату не должно активировать его раньше срока, а бессрочные шаблоны
      не должны быть единственным вариантом.
- [x] Уточнить «Эту и будущие»: обновлять не только шаблон, но и уже материализованные
      будущие pending-задачи, если они существуют; отменённые и завершённые не менять.
- [x] Сохранять выбранную дату экрана «Сегодня» при переходах между разделами, как требует
      `SPEC.md`, а не всегда возвращать пользователя на текущую дату.

### Квесты

- [x] Добавить заявленный в `SPEC.md` фильтр квестов по типу: основной, побочный,
      долгосрочный; сохранять выбранный фильтр вместе со вкладкой.
- [x] Определить жизненный цикл `draft`: либо реализовать «Сохранить как черновик» и
      последующую активацию, либо удалить неиспользуемый статус из UI/API/модели.
- [x] Сделать дедлайн функциональным: выделять просроченные квесты, сортировать ближайшие
      дедлайны и определить, остаётся ли просроченный квест доступным для завершения.
- [x] Разрешить убирать завершённые квесты из постоянно растущего списка: архивировать их
      без потери истории или добавить отдельное скрытие.

### Навыки, шаблоны и архивы

- [x] Добавить экран/блок архивных навыков и восстановление. Сейчас навык после архивации
      исчезает из интерфейса без пользовательского способа вернуть его.
- [x] Добавить восстановление архивных шаблонов и полноценные действия в архиве. Сейчас
      архив отображается только как текстовый список.
- [x] При восстановлении навыка или шаблона обрабатывать конфликт нормализованного названия:
      предложить переименование либо оставить запись в архиве с понятной ошибкой.

### Контент-пак v2

- [ ] Расширить контент-пак разовыми задачами: текущий формат импортирует только навыки,
      повторяющиеся действия и квесты, хотя стартовый пакет предполагает разовые задания.
- [ ] Добавить в формат длительность и переносимые даты: `estimatedMinutes`, относительный
      дедлайн (`dueInDays`) и при необходимости относительную дату разовой задачи вместо
      быстро устаревающих абсолютных дат.
- [ ] Добавить предварительный просмотр импорта: что будет создано, пропущено и отклонено;
      затем отдельное подтверждение. В будущем — выбор импортируемых разделов без нарушения
      атомарности зависимых записей.
- [ ] Описать совместимость и миграцию версий контент-пака, чтобы появление формата v2 не
      ломало уже созданные пакеты v1.

## P2 — обзорность и повседневный UX

- [ ] Добавить ручной порядок или приоритет задач дня. Сейчас pending-задачи сортируются
      в основном по времени создания, что не помогает планировать последовательность работы.
- [ ] Добавить поиск и компактные фильтры для навыков, шаблонов и квестов до того, как
      стартовый контент заметно увеличит списки.
- [ ] Показывать серии по конкретным шаблонам, а не только максимальную текущую/лучшую серию
      на экране прогресса.
- [ ] Добавить простой недельный обзор без AI: XP, выполненные и пропущенные действия,
      изменения серий, завершённые и зависшие квесты, основные направления прогресса.

## P0 — корректность и целостность — ✅ выполнено

- [x] Запретить ручное завершение квеста, пока не выполнены все обязательные шаги.
- [x] Запретить изменение шагов завершённого или архивного квеста на сервере.
- [x] Требовать у квеста минимум один шаг, среди которых минимум один обязательный.
- [x] Исправить период `all` на экране прогресса: `fillDailyRange` ограничен 400 днями,
      поэтому длинная история и итоговый `totalXp` обрезаются и могут не включать последние дни.
- [x] Сделать текущую серию актуальной относительно сегодняшней даты. Сейчас она остаётся
      равной последней накопленной серии даже после длительного пропуска.
- [x] Определить правила серии для шаблонов по дням недели. Текущий расчёт требует
      последовательных календарных дней, поэтому расписание Пн/Ср/Пт почти всегда имеет серию 1.
- [x] Запретить или корректно мигрировать смену характеристики навыка после начисления XP.
      API умеет менять `attributeId`, но исторический XP остаётся в старой характеристике,
      а новые начисления идут в новую.
- [x] Определить каскад при архивировании навыка. Активные шаблоны этого навыка продолжают
      создавать задачи, хотя сам навык скрыт из активных списков.
- [x] Добавить связанную валидацию PATCH шаблона: для `weekdays` обязательны дни недели,
      при переходе на `daily` старый массив дней должен очищаться.
- [x] Проверять реальную календарную дату, а не только формат `YYYY-MM-DD`, для задач,
      шаблонов и дедлайнов квестов. Значения вроде `2026-02-31` сейчас проходят Zod и падают
      позже на уровне даты или PostgreSQL.
- [x] Разделить liveness и readiness. `/api/health` возвращает `ok` без проверки БД,
      поэтому контейнер считается здоровым при недоступном PostgreSQL.
- [x] Добавить production-проверку обязательных переменных окружения. `DATABASE_URL`,
      `TELEGRAM_BOT_TOKEN`, allowlist и `APP_URL` остаются optional в общей env-схеме;
      ошибка обнаруживается только при первом реальном запросе.

## Выполнено — игровые механики и пользовательские сценарии

### Задачи и шаблоны

- [x] Реализовать просмотр другой даты или запретить создание разовой задачи не на сегодня.
      Сейчас можно выбрать прошлую или будущую дату, после чего задача недоступна в UI.
- [x] Реализовать редактирование и отмену/удаление задачи (`PATCH`/`DELETE /api/tasks/:id`),
      заявленные в `SPEC.md`. Статус `cancelled` в модели сейчас не используется.
- [x] При редактировании задачи из шаблона добавить выбор «только эта задача» / «будущие
      задачи шаблона».
- [x] Добавить полноценное управление шаблонами: редактирование, пауза, возобновление,
      просмотр расписания и архива. В профиле доступно только архивирование активного шаблона.
- [x] Показывать описание и оценку длительности задачи. Поля есть в API/БД, но длительность
      отсутствует в форме, а описание не выводится в карточке.
- [x] Решить, нужны ли отдельные базовые XP и сложность одновременно: пользователь вручную
      управляет обоими параметрами и может легко нарушить баланс.
- [x] Унифицировать диапазоны XP: `SPEC.md`, UI-константы и API допускают разные пределы
      (`5–250`, `1–1000`, награда квеста до `10000`).

### Квесты

- [x] Добавить редактирование, архивирование и восстановление квеста в UI.
- [x] Вывести в форму квеста уже поддерживаемые моделью поля: характеристику, дедлайн,
      ручное/автоматическое завершение и обязательность шагов.
- [x] Реализовать добавление, редактирование, удаление и сортировку шагов после создания
      квеста; вывести описание шага.
- [x] Согласовать progress bar и завершение квеста по обязательным шагам.
- [x] Связать шаг квеста с задачей дня: создание задачи и закрытие шага при выполнении.
- [x] Показывать характеристику и дедлайн квеста в списке и деталях после добавления этих
      полей в форму.

### Навыки и достижения

- [x] Определить UX смены характеристики навыка: разрешать только до первого XP,
      переносить историю или полностью запретить.
- [x] Подключить предусмотренные схемой `icon` и `color` либо удалить их из модели и
      спецификации как неиспользуемые поля.
- [x] Показывать последствия отмены выполнения: уже открытые достижения остаются
      открытыми, хотя XP и серия откатываются.
- [x] Сделать карточки достижений полезными на мобильном устройстве. Описание доступно
      только через HTML `title`, которого фактически нет в touch-интерфейсе.

### Прогресс и данные

- [x] Согласовать смысл «XP за период»: сейчас учитываются только выполнения задач,
      а награды квестов видны в общем XP и журнале, но не входят в график и итог периода.
- [x] Применять выбранный период к распределению по характеристикам либо явно подписать,
      что этот блок всегда показывает значения за всё время.
- [x] Группировать три XP-транзакции одной задачи в одно понятное событие и показывать
      источник, название действия/квеста и дату. Сейчас журнал выглядит как несколько
      обезличенных начислений.
- [x] Добавить импорт собственного JSON-экспорта и версионирование формата экспорта.
- [x] Добавить массовый импорт контент-пака: навыки, повторяющиеся действия и квесты.
      Это позволит сначала генерировать и проверять контент в Markdown/JSON, затем загружать
      его без ручного заполнения каждой формы.
- [x] Добавить проверку дубликатов и конфликтов при импорте контента.

## P1 — Telegram, тестирование и эксплуатация

- [ ] Проверить и выбрать стратегию cookie для Telegram Desktop/Web. `SameSite=Lax`
      может не работать внутри cross-site iframe; сейчас официально поддержан только основной
      мобильный сценарий.
- [ ] Синхронизировать тему с `Telegram.WebApp.colorScheme` и вызывать `expand()` при
      необходимости. Сейчас используется только системная тема браузера.
- [ ] Учесть верхний и нижний Telegram safe area не только в bottom navigation, но и в
      основном layout, Drawer и диалогах.
- [ ] Расширить E2E до сценариев из `SPEC.md`: создание навыка и повторения, выполнение и
      откат задачи, создание/прохождение квеста, статистика, запрет чужого пользователя.
      Сейчас E2E проверяет health, навигацию и создание разовой задачи через dev bypass.
- [ ] Добавить мобильный Playwright-проект и проверки ширины 320 px/safe area. Текущая
      конфигурация запускает только Desktop Chrome.
- [ ] Добавить отдельную проверку реального Telegram auth flow или максимально близкий
      браузерный fixture вместо покрытия E2E только через dev bypass.
- [ ] Автоматизировать off-site копирование бэкапов, проверку checksum, шифрование и
      регулярный тест восстановления. Текущий скрипт создаёт и ротирует только локальные копии.
- [ ] Добавить структурированные логи и минимальные метрики ошибок/латентности. Сейчас
      используются отдельные `console.error`, хотя `SPEC.md` требует структурированное логирование.
- [ ] Усилить CSRF-проверку в production: определить политику для запросов без `Origin`
      и при необходимости проверять `Sec-Fetch-Site`.
- [ ] Добавить CSP, совместимую с Telegram Mini App и внешним Telegram SDK, не ломая iframe.
- [ ] Добавить обслуживание таблицы сессий: удаление истёкших/отозванных записей.

## P2 — UX и доступность

- [x] Вернуть пользователю конкретные ошибки API вместо одинаковых сообщений
      «Не удалось…» для валидации, конфликта и сетевой ошибки.
- [x] Не отключать масштабирование страницы (`userScalable=false`) либо обосновать это
      отдельным accessibility-решением.
- [x] Проверить минимальную touch-область 44×44 px. Часть кнопок и переключатели дней
      недели меньше требования из `SPEC.md`.
- [x] Добавить `prefers-reduced-motion`/`motion-reduce` для Drawer, Dialog, progress bar
      и спиннеров.
- [x] Сделать выбор часового пояса полным: UI предлагает только шесть зон, хотя backend
      принимает любую валидную IANA timezone.
- [x] Обрабатывать ошибку сохранения темы: сейчас тема меняется локально до ответа API,
      а неуспешная запись никак не показывается.
- [x] Сохранять ожидаемую позицию списков и состояние вкладок при возврате из деталей.

## P2 — документация и технический долг

- [ ] Удалить устаревшие заметки в `TASKS.md`: там всё ещё указано, что не реализованы
      «Эту и будущие», создание задачи из шага квеста и учёт наград квестов в дневной
      статистике, а смена характеристики навыка якобы доступна только при создании.
- [x] Синхронизировать `SPEC.md` с фактическим API: объяснить отсутствие `/api/today`,
      задокументировать `PATCH`/`DELETE /api/tasks/:id` и реализованные ограничения.
- [x] Обновить roadmap-чекбоксы в `SPEC.md` или явно обозначить, что актуальный статус
      ведётся только в `TASKS.md`.
- [x] Выпустить первую версию changelog вместо хранения всего MVP в `[Unreleased]`.
- [x] Удалить устаревшие stage-комментарии в `env.ts`, `complete-task.ts` и schema-файлах.
- [x] Добавить DB-level `CHECK`/enum-ограничения для статусов, типов, сложности и XP,
      чтобы целостность не зависела только от Route Handler и Zod.
- [x] Решить политику уникальности пользовательских навыков и шаблонов, чтобы случайные
      дубликаты не размывали статистику и контент.

## Идеи контента

Не реализовывать без отдельного запроса.

- [ ] Сначала описать единую шкалу XP по времени, усилию, дискомфорту и ценности результата.
      Использовать её как обязательную основу для задач, шаблонов и наград квестов, чтобы
      сгенерированный контент не ломал баланс.
- [ ] Подготовить стартовый контент-пак по шести характеристикам: навыки, ежедневные и
      недельные действия, разовые задания и первые квесты.
- [ ] Добавить библиотеку шаблонов квестов: здоровье, обучение, работа, финансы,
      отношения, личные проекты.
- [ ] Добавить мягкий первичный выбор направлений, чтобы не создавать пользователю весь
      набор контента автоматически. Выбор должен импортировать только подходящие пресеты и
      не превращаться в длинный onboarding.
- [ ] После стабилизации формата добавить встроенную библиотеку контент-паков с описанием,
      предпросмотром, версией и явным составом вместо обязательной загрузки JSON-файла.
- [ ] В будущем рассмотреть главы, боссов, титулы, перки, временные эффекты и интеграции из
      `SPEC.md` §5.9 только после стабилизации основного контента и недельных обзоров.
