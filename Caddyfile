# APP_DOMAIN is read from .env. Defaults to localhost for local runs;
# set it to your real domain in production to get automatic HTTPS.
{$APP_DOMAIN:localhost} {
	encode zstd gzip
	reverse_proxy app:3000
}
